name: Publish Beta Package

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # required for nuget.org Trusted Publishing (OIDC)
      contents: read

    steps:
    - uses: actions/checkout@v7
      with:
        fetch-depth: 0

    - name: List directory structure
      run: |
        echo "Current directory:"
        pwd
        echo "Directory contents:"
        ls -la
        echo "Src directory contents:"
        ls -la src
        echo "ShowsOnsale.World directory contents:"
        ls -la src/ShowsOnSale.World

    - name: Setup .NET 10.0
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '10.0.x'

    - name: Check .NET SDK
      run: |
        echo "Dotnet version:"
        dotnet --version
        echo "Dotnet info:"
        dotnet --info
        echo "Dotnet location:"
        which dotnet

    - name: Get version
      id: get_version
      run: |
        # Get the latest tag or default to 0.0.0
        LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "0.0.0")
        # Remove 'v' prefix if it exists
        LATEST_TAG=${LATEST_TAG#v}
        # Split version into major.minor.patch
        IFS='.' read -r major minor patch <<< "$LATEST_TAG"
        # Increment patch version
        patch=$((patch + 1))
        # Construct new version
        NEW_VERSION="${major}.${minor}.${patch}"
        echo "VERSION=${NEW_VERSION}" >> $GITHUB_ENV
        echo "BUILD_NUMBER=${{ github.run_number }}" >> $GITHUB_ENV
        echo "BETA_VERSION=${NEW_VERSION}-beta.${{ github.run_number }}" >> $GITHUB_ENV

    - name: Build and pack
      run: |
        dotnet build src/ShowsOnSale.World/ShowsOnSale.World.csproj --configuration Release
        dotnet pack src/ShowsOnSale.World/ShowsOnSale.World.csproj --configuration Release --no-build -p:Version=${{ env.BETA_VERSION }}

    - name: NuGet login (OIDC -> short-lived API key)
      if: github.event_name == 'push'
      uses: NuGet/login@v1
      id: login
      with:
        user: ${{ secrets.NUGET_USER }}   # your nuget.org username (profile name), NOT email

    - name: Push to NuGet
      if: github.event_name == 'push'
      run: |
        dotnet nuget push "src/ShowsOnSale.World/bin/Release/ShowsOnSale.World.${{ env.BETA_VERSION }}.nupkg" --api-key ${{ steps.login.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
